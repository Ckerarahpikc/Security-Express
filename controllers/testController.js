const mongoose = require("mongoose");
const User = require("../models/testModel");
const catchAsync = require("../utils/catchAsync");

// * insert
exports.goInsert = catchAsync(async (req, res, next) => {});
